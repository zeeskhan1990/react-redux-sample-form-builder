/*
 * SourceCard Messages
 *
 * This contains all the text for the SourceCard component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.SourceCard';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SourceCard component!',
  },
});
